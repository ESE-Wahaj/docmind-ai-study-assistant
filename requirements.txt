streamlit==1.58.0
google-genai==2.8.0
chromadb==1.5.9
pypdf==6.14.2
python-docx==1.2.0
python-dotenv==1.0.1
pydantic==2.9.2

# ChromaDB needs a modern SQLite (>=3.35). Streamlit Community Cloud's base
# image ships an older system SQLite, so we swap it for a bundled build at
# runtime (see the top of app.py). Only needed on Linux deployment targets;
# harmless to install locally too.
pysqlite3-binary==0.5.3; sys_platform == "linux"
